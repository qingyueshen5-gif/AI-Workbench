import assert from 'node:assert/strict';import fs from 'node:fs/promises';
const source=await fs.readFile(new URL('../agents/agent-runtime.mjs',import.meta.url),'utf8');
for(const forbidden of ['statusPattern','const directRead','read_file_started','status_grounding_started',"toolUsed: 'read_file'","provider: 'local-status'","provider: 'local-read'"])assert.equal(source.includes(forbidden),false,`legacy shortcut remains: ${forbidden}`);
assert.equal((source.match(/const groundedCapabilities =/g)||[]).length,1);assert.match(source,/executeWithRun/);assert.match(source,/bindRunVerification/);assert.match(source,/finalizeRun/);
const {spawnSync}=await import('node:child_process');const bypass=spawnSync(process.execPath,['scripts/verify-interpreter-adapter-bypass-001.mjs'],{encoding:'utf8'});assert.equal(bypass.status,0,bypass.stderr||bypass.stdout);const integration=spawnSync(process.execPath,['scripts/verify-interpreter-adapter-b5-integration-001.mjs'],{encoding:'utf8'});assert.equal(integration.status,0,integration.stderr||integration.stdout);const parsed=JSON.parse(integration.stdout);assert.equal(parsed.runtimeStatus.finalCount,1);assert.equal(parsed.fileRead.finalCount,1);assert.equal(parsed.metrics.modelCalls,0);
console.log(JSON.stringify({ok:true,module:'INTERPRETER-ADAPTER-B5-S4-REMOVE-EXECUTION-SHORTCUTS',legacyStatusRemoved:true,legacyDirectReadRemoved:true,runtimeStatusReachablePaths:1,fileReadReachablePaths:1,duplicateAssistantReplies:0,nonExecutionBypass:'PASS',integration:'PASS'},null,2));
