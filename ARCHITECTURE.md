# ARCHITECTURE.md — AI Workbench 产品架构

> 本文件是模块 04、05、06、11、12、18、21 的唯一架构权威。产品原则见 `PRINCIPLES.md`，执行和测试规则见 `EXECUTION_PROTOCOL.md`，当前实现完成度见 `CURRENT_PROGRESS_AUDIT.md`。

## 04. 总体架构

AI Workbench 的目标架构是：

`用户/通讯入口 → 统一任务入口 → 身份与风险检查 → 上下文与任务引擎 → 调度器 → 模型/Agent/工具执行器 → 验证与证据 → 结果返回 → 状态与记忆写回`

核心由以下层组成：

1. **入口层**：桌面输入框、未来 Web/移动端及通讯适配器。
2. **Gateway 层**：统一接收任务、身份、权限、限流和状态查询。
3. **上下文与任务层**：读取必要记忆、补全目标、拆分任务、管理依赖和生命周期。
4. **调度层**：选择规则、脚本、模型、Agent、Provider 和执行器。
5. **执行层**：Codex、Hermes、模型、浏览器、文件、终端、搜索及其他工具适配器。
6. **验证层**：检查真实产物、证据、风险和完成条件。
7. **恢复层**：重试、换路由、降级、续跑、回滚和人工接管。
8. **状态与证据层**：保存任务、运行、成本、日志、证据、决策和可审计记忆。

当前代码可以逐步演进，但新增 Provider、Agent 或入口不得绕开统一任务、权限、预算、验证和证据层。

## 核心数据归属

中央状态归 AI Workbench，不归单个模型或 Agent：

- `agents`：能力、权限、成本、健康和适配器；
- `tasks`：用户目标、拆解、依赖、风险和状态；
- `runs`：每次执行、输入摘要、输出、成本、错误和证据；
- `memories`：用户偏好、项目上下文、决策、错误经验和环境状态；
- `approvals`：高风险动作的逐次授权；
- `traces`：跨组件链路、重试、降级和恢复记录。

执行者只获得当前任务所需的最小上下文和权限，不能各自维护不可见的项目真相。

## 05. Agent 体系

### 角色类型

- **协调 Agent**：无隐藏长期记忆，只读取文件和任务状态，负责派活、跟踪、汇总和把高风险事项弹回产品负责人。
- **总监工/产品 Agent**：检查目标、范围、原则和产品价值，不直接替产品负责人拍板。
- **执行 Agent**：在限定权限内操作代码、文件、浏览器、终端或外部服务。
- **测试 Agent**：独立验证产物和失败路径，不接受执行者自报成功。
- **总集成 Agent/Codex**：统一合并、运行测试、检查 diff、生成证据、commit 和 push；不自动部署或改变产品方向。

### Agent 接入契约

每个 Agent/工具至少声明：身份、能力、权限、成本等级、风险等级、健康检查、输入输出格式、取消方式、超时、证据类型和错误归一化方式。

协调者不能批准支付、删除、外发、跨账号、生产部署或其他高风险动作。所有 Agent 结果都必须经过验证层，不能免检进入最终状态或 `main`。

## 06. 模型与 Provider 调度

调度优先顺序：

1. 确定性规则或脚本；
2. 低成本模型处理简单、重复、批量和格式化任务；
3. 高质量模型处理战略、架构、安全、模糊判断和重要复核；
4. 需要真实操作时调用对应 Agent 或工具；
5. 失败后根据错误类型重试、换 Provider、换模型、换 Agent 或降级。

调度器综合任务难度、风险、质量目标、成本预算、上下文长度、时限、隐私和当前健康状态。用户不负责选模型，但系统必须记录选择原因、Token/费用估计、调用和失败历史。

Provider 必须经统一适配和可控通道。当前生产链路以本地 `18800` 与 AI Workbench Managed Proxy 为基础，实际生产实现和状态以 `CURRENT_PROGRESS_AUDIT.md` 与对应 verification 为准。

## 11. 故障诊断

所有故障使用统一链路：

`故障现象 → 影响范围 → 原因假设 → 定位动作 → 原始证据 → 根因 → 解决动作 → 验证 → 自动检测建议`

诊断必须区分用户可见解释和内部原始证据。用户看到发生了什么、系统检查了什么、已经做了什么、还能怎么继续；内部保存错误码、Trace、日志、组件状态和重试记录。

真实 Bug 个案继续保存在 `tasks/` 和 `verification/`，不把历史故障逐条复制进本架构文件。可重复故障应沉淀为健康检查、错误分类或自动修复规则。

## 12. 环境兼容与低资源设计

产品不得假设高配电脑、稳定网络或熟练用户。架构必须支持：

- Windows 当前主平台的安装、升级、卸载和恢复；
- 低内存、低磁盘、弱 CPU、差网络和代理波动；
- 常驻进程、缓存、日志和临时文件的资源上限与清理；
- 路径、权限、依赖、端口和配置迁移；
- 核心能力不可用时的降级状态，而不是直接崩溃；
- 多系统适配先保留接口与数据边界，国际化阶段再按路线实施。

环境问题应进入健康状态和恢复流程，不依赖用户阅读原始报错。

## 18. 飞书与 AI Link 集成

通讯和内部提速拆成两条独立线：

### A. 外部用户通讯入口

面向用户的飞书、微信、Telegram、WhatsApp、Discord 等入口，只负责接收目标、身份和授权，调用统一任务入口，再把状态与结果返回原会话。AI Workbench 核心不绑定任何单一平台。

该线当前是否开工和版本归属只看 `CURRENT_PROGRESS_AUDIT.md`。

### B. 内部研发指挥与提速通道

飞书或 AI Link 可作为产品负责人从手机创建、批准、查看内部任务的通道，但不能绕过任务状态机、预算、权限、Git 边界、测试和最终集成。

内部飞书当前架构边界：自有企业应用、官方 SDK、WebSocket 长连接、owner allowlist 或一次性配对、凭据仅本机安全保存；渠道只调用任务网关，不直接创建 worktree、启动未批准任务、修改 Git、部署生产或发送任意目标消息。

AI Link 只作为受控连接和提速候选，开工前必须明确日成本、权限、数据流、失败恢复和替代路径。

## 21. 故障恢复

恢复层处理 Provider、Gateway、AI Link、Agent、网络、登录、依赖和本地环境故障：

- 短暂错误：退避重试并限制次数；
- 单 Provider/模型失败：切换备用或降级能力；
- Gateway/AI Link 失败：保留任务状态，转本地或备用入口；
- Agent 掉线或超时：取消孤儿运行，换执行者或等待人工接管；
- 登录或权限过期：停止敏感动作，提供清晰恢复入口；
- 应用重启：从持久化 task/run 状态续跑，不靠聊天记忆；
- 预算、安全或证据异常：fail closed，不继续调用；
- 无法自动恢复：给出已完成部分、阻塞原因、剩余动作和人工选项。

关键任务必须定义恢复点、幂等边界、重复执行风险和最终验证。自动恢复不能跨越新的支付、删除、外发或权限授权。

## 安全、成本、测试和进度引用

- 安全与成本铁律：`PRINCIPLES.md`
- 工程、日志、证据、测试和连续性：`EXECUTION_PROTOCOL.md`
- 已锁定架构决定：`DECISIONS.md`
- 当前实现、缺口和路线：`CURRENT_PROGRESS_AUDIT.md`
