# PRINCIPLES.md — 设计原则

## 1. 为自己而做

不是做一个"给别人看"的产品。每个功能都解决我自己的真实痛点。

## 2. 简单胜过完备

一个能做一件事的脚本，胜过十个"支持所有场景"的框架。

```
好: python summarize.py article.txt
坏: python main.py --mode=summarize --input=article.txt --output=result.json --format=markdown --model=gpt-4
```

后台可以复杂，用户面前必须简单。用户只表达目标，系统负责拆解、选择模型、调用工具、检查、修复和交付。

## 3. 文本即接口

输入是文本，输出是文本。不定义新格式，不造新协议。纯文本天然可以被任何工具消费。

## 4. 先跑起来再优化

不要"设计好了再动手"。用最糙的方式先跑通，用了几天觉得哪里不爽再改。

## 5. 本地优先，隐私第一

- 用户数据、任务状态和执行证据本地优先保存
- 真实模型 key 不进入安装包、前端、员工配置或用户电脑
- 模型请求可以通过 AI Workbench 自控 Managed Proxy 调用
- 不把用户数据上传到无关第三方服务

## 6. 可还原

- 每个功能有清晰的输入 → 输出映射
- 同样的输入，同样的输出
- 出错了能复现

## 7. 真实完成

- 不把“说了怎么做”当成“做完了”。
- 关键动作要有证据、状态和失败原因。
- 失败时给中文人话解释，能恢复就恢复，不能恢复就说清阻塞点。

## 8. 高质量、快速、低损耗

- 结果必须简单、清楚、可执行。
- 避免不必要的模型调用、Token 浪费和重复劳动。
- 能用便宜稳定路径解决的，不默认上复杂路径。
- 质量检查、成本控制和执行证据必须属于工作台控制层。

## 9. 透明可追溯

- README.md 让人知道这是什么
- 代码里的注释解释为什么这样做
- 不写"完整文档"——没人看
- 当前进度、历史决策、验收证据和下一步必须能被新对话读取并接手。

## 10. 了解与开发分离

- 对前沿方向的了解、学习和情报收集，属于喂养产品负责人判断的活动，允许且鼓励。
- 了解不等于开发。当前地基稳固、成本可控、有真实用户之前，不得启动前沿方向的实际工程开发。
- 具身智能、复杂执行体、前沿 Agent 框架和类似方向可以持续观察，但不得消耗当前产品开发资源，不得抢占当前唯一主线。
- 学习是为了将来加速，不是为了现在分心。

## 11. 简单粗暴模式与细致模式的切换原则

- 默认状态是简单粗暴：用户提出诉求，工作台直接去办，不多问、不磨叽，尽量用一个输入框完成。这是产品体验的主旋律。
- 遇到墙时先分类，再决定模式。
- 技术保护墙：平台明确设置的限制，例如验证码、反爬、安全边界、权限边界。遇到这类墙时切换为细致模式，不硬闯，走官方渠道，需要授权时明确请求授权，绝不绕过。
- 信息分散墙：信息本身公开，只是散落在多个地方、没有被整理。遇到这类墙时仍用简单粗暴模式处理，直接采集、归纳和汇总，不把普通整理工作过度复杂化。
- 这条分界线是产品能同时做到“快”和“让人放心用”的原因：日常操作足够快，触及红线时足够谨慎。
- 长期延伸到具身智能等物理世界执行能力时，仍沿用同一套切换逻辑，而不是另立新原则。物理世界风险更高，因此这套切换机制的重要性只增不减。
- 这不是纸面原则。项目本身的开发方式，包括备份先行、小流量验证、真实调用前反复核对、异常即停不硬推，就是细致模式在实践中的体现。产品对外部世界的行为准则，与团队对内的工作方式，是同一套逻辑。
