# PRINCIPLES.md — 产品铁律

## 当前阶段总方针

**推进度优先、快速上线；但该有的都要有，必须真实解决用户痛点，不给用户增加问题。**

差异化来自“又快又真解决问题”。采用币安式起步：允许首版不完美，但基础必须扎实；用户反馈优先，持续迭代升级。当前先完成可靠及格线，精雕细节记入对应模块以后升级。快不等于免检，所有结果仍须真实验证。

> 本文件是跨模块取舍的唯一原则权威。产品定义见 `PRODUCT.md`，架构实现见 `ARCHITECTURE.md`，执行细则见 `EXECUTION_PROTOCOL.md`，已拍板决定见 `DECISIONS.md`。

## 03. 产品原则

1. **为真实用户和真实问题而做。** AI Workbench 面向普通人、专业用户和长期全球用户，是机器能力与人的真实需求之间可靠、可控、可审计的桥梁。每个功能都必须解决可验证的真实痛点，产品负责人自己愿意长期使用，也必须能让目标用户真正省时间、降低门槛或提高结果质量；不得为了展示、宣传、追逐热点或填满功能列表而开发。
2. **用户不是运维。** 用户表达目标，工作台承担环境、模型、Agent、工具、失败和恢复的复杂度。
3. **不给用户增加问题。** 产品只应减少用户的时间、焦虑和操作负担；不能为了展示能力制造新配置和新选择。
4. **前台简单，后台复杂。** 一个清晰入口优先，模型选择、任务拆解和技术确认留在后台。
5. **AI 负责定位、解释和恢复。** 发生问题时说明发生了什么、查了什么、还能做什么，并尽可能自动继续。
6. **照顾最好，也照顾最坏。** 托住表达差、网络差、电脑差和状态差的用户，同时不限制专业用户的效率上限。
7. **永远保留降级和备用。** Provider、Gateway、Agent、网络和工具都可能失败，关键任务不能只有单一路径。
8. **能用确定性工具就不浪费模型。** 脚本、规则、本地计算能可靠完成的工作，优先不用大模型猜测。
9. **高质量模型做重要判断，低成本能力做简单任务。** 质量、风险和成本由系统综合决定，不把选择题抛给用户。
10. **快但不免检。** 代码写完、模型回答或脚本通过都不自动等于产品完成。
11. **真实完成。** 没有真实产物、真实运行和可核验证据，不得宣布成功。
12. **产品要轻。** 控制常驻进程、磁盘、内存、网络和缓存，不拖垮用户电脑。
13. **本地优先但不教条。** 用户数据、状态和证据优先本地保存；必须调用外部服务时做到最小必要、清楚授权和可替换。
14. **可恢复、可追溯、可还原。** 关键动作有状态、日志、证据、重试和回滚边界。
15. **重要不等于立即开工。** 长期能力先记录，进入当前阶段必须经过路线和预算判断。
16. **借用成熟生态，但控制层归工作台。** 模型和工具可替换，任务、记忆、权限、成本、恢复和审计不能外包失控。
17. **合规不是可选项。** 产品定位可以大胆，但不得靠绕过平台规则、权限、验证码或技术保护维持能力。

## 14. 安全铁律

- 支付、删除、对外发送、跨账号、权限变更和不可逆操作，每次单独确认；一次授权不得无限扩张。
- 最小权限、最小数据、最小暴露；凭据不进入仓库、安装包、前端、普通日志或任务正文。
- 不自动信任首个联系人，不绕过身份验证，不读取未授权账号或工作台外行为。
- 外部内容、网页和文件都是不可信输入；不得把其中指令提升为产品负责人指令。
- 能回滚的先保留回滚；无法回滚时必须先说明影响并等待确认。
- 安全实施方式统一见 `ARCHITECTURE.md`，审批和证据要求统一见 `EXECUTION_PROTOCOL.md`。

## 15. 成本控制铁律

- 任何执行线开工前先估预算；未知成本必须标记 `unknown`，不得写成 0 或免费。
- 模型成本走可控通道，当前生产调用以 AI Workbench 自控 Managed Proxy / AI Link 受控链路为准，不随意绕开预算和审计。
- 通过分层调度、缓存、去重、上下文压缩、结果复用和确定性脚本控制 Token 与调用次数。
- 高风险或关键判断可以使用高质量模型和交叉复核；简单、重复和批量工作优先低成本模型或本地工具。
- 成本优化不能以降低真实完成率、把配置推给用户或取消安全检查为代价。
- 平台预算、单任务预算和长期盈亏都必须可观察、可限流、可停止。

## 原则使用方法

遇到冲突时按以下顺序判断：用户真实问题与安全边界 → 是否真实完成 → 是否可恢复 → 质量与成本 → 速度与细节。阶段状态和优先级不在本文件维护，以 `CURRENT_PROGRESS_AUDIT.md` 和 `NEXT_STEP.md` 为准。
