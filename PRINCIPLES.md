# PRINCIPLES.md — 设计原则

## 1. 为自己而做

不是做一个"给别人看"的产品。每个功能都解决我自己的真实痛点。

## 2. 简单胜过完备

一个能做一件事的脚本，胜过十个"支持所有场景"的框架。

```
好: python summarize.py article.txt
坏: python main.py --mode=summarize --input=article.txt --output=result.json --format=markdown --model=gpt-4
```

后台可以复杂，用户面前必须简单。用户只表达目标，系统负责拆解、选择模型、调用工具、检查、修复和交付。

## 3. 文本即接口

输入是文本，输出是文本。不定义新格式，不造新协议。纯文本天然可以被任何工具消费。

## 4. 先跑起来再优化

不要"设计好了再动手"。用最糙的方式先跑通，用了几天觉得哪里不爽再改。

## 5. 本地优先，隐私第一

- 用户数据、任务状态和执行证据本地优先保存
- 真实模型 key 不进入安装包、前端、员工配置或用户电脑
- 模型请求可以通过 AI Workbench 自控 Managed Proxy 调用
- 不把用户数据上传到无关第三方服务

## 6. 可还原

- 每个功能有清晰的输入 → 输出映射
- 同样的输入，同样的输出
- 出错了能复现

## 7. 真实完成

- 不把“说了怎么做”当成“做完了”。
- 关键动作要有证据、状态和失败原因。
- 失败时给中文人话解释，能恢复就恢复，不能恢复就说清阻塞点。

## 8. 高质量、快速、低损耗

- 结果必须简单、清楚、可执行。
- 避免不必要的模型调用、Token 浪费和重复劳动。
- 能用便宜稳定路径解决的，不默认上复杂路径。
- 质量检查、成本控制和执行证据必须属于工作台控制层。

## 9. 透明可追溯

- README.md 让人知道这是什么
- 代码里的注释解释为什么这样做
- 不写"完整文档"——没人看
- 当前进度、历史决策、验收证据和下一步必须能被新对话读取并接手。

## 10. 了解与开发分离

- 对前沿方向的了解、学习和情报收集，属于喂养产品负责人判断的活动，允许且鼓励。
- 了解不等于开发。当前地基稳固、成本可控、有真实用户之前，不得启动前沿方向的实际工程开发。
- 具身智能、复杂执行体、前沿 Agent 框架和类似方向可以持续观察，但不得消耗当前产品开发资源，不得抢占当前唯一主线。
- 学习是为了将来加速，不是为了现在分心。

## 11. 简单模式与细致模式的切换原则

- 默认采用简单直接模式：用户提出诉求，工作台直接执行，不进行没有必要的追问，不把简单事情复杂化，尽量用一个输入框完成。
- 遇到“墙”时先分类，再决定模式。
- 技术保护墙包括验证码、反爬限制、登录授权、权限边界、安全限制、平台明确禁止的行为、法律与合规边界。遇到这类墙时切换细致模式：不硬闯、不绕过，使用官方接口和官方渠道，需要用户授权时明确请求，无法合法完成时停止并说明。
- 信息分散墙指信息本身公开、合法，但分散在多个来源、未被整理。遇到这类墙时继续使用简单直接模式：搜索、采集、比较、整理和汇总，不把正常的信息整理误判成安全限制。
- 这条分界线是产品同时做到“快”和“让人放心用”的原因：日常公开信息整理足够快，触及保护边界时足够谨慎。
- 未来如果产品进入具身智能或物理世界执行，继续使用同一套模式切换逻辑。物理世界风险更高，因此细致模式的重要性只会增加。
- 这不是纸面原则。项目已经采用的备份先行、小流量灰度、真实调用前核对、异常立即停止和保留回滚能力，就是细致模式的实际应用。产品对外部世界的行为准则，与团队对内的工作方式，是同一套逻辑。

## 12. 单任务推进原则

- 产品负责人可以在思考层面同时关注多个方向和多个卡点。这是产品负责人的判断优势，不视为需要改正的问题。
- 但在执行层面，任何时候只允许一条任务线真正开工，不得同时让 Codex 执行两条及以上任务。
- 新想法先记录，不立刻开工。当前任务完成、验收并停止后，才能开始下一任务。
- 这样做是为了减少注意力切换、保护判断质量、防止项目来回跳转、防止多个未验收改动互相污染。
- 产品负责人的判断力是项目最不可替代的输入。当前唯一下一步始终以 `NEXT_STEP.md` 为准。

### 重要性与当前优先级分离

- 重要方向不自动等于当前任务。
- 新想法默认先记录，不自动开工。
- 当前唯一任务只有产品负责人可以明确改变。
- 上线及格线完成前，不用满分线功能阻塞首批用户试用。
- 真实用户出现后，再根据实际反馈排序后续候选模块。

## 13. 优先借用成熟生态

- AI Workbench 不是只优化某一个模型的客户端。产品定位是调度可调用的模型、Agent、成熟工具，以及未来可接入的执行能力。
- 不仅执行层要借用成熟生态，支持性能力也一样，包括信息与情报收集、用户反馈整理、浏览器自动化、安全扫描、依赖检测、日志与监控、支付能力、未来物理世界执行工具。
- 优先顺序固定为：先寻找成熟产品，再寻找可靠开源工具，优先组合和接入，只有现有方案确实不能满足产品边界、质量、安全、成本或可控性要求时才自行开发。
- 工作台真正要掌握的是控制层：任务状态、长期记忆、任务分配、质量检查、成本控制、执行证据、失败恢复和最终结果审计。
- 不得为了证明技术能力而重复造轮子。

## 14. 个人支付与产品支付分离

- 产品负责人个人的支付工具，例如个人信用卡、个人借记卡、Visa 卡、支付 AI 工具订阅的个人支付渠道，属于个人事务，可以独立研究和处理，不属于 AI Workbench 产品架构。
- AI Workbench 未来向用户收款的支付系统属于产品能力。
- 当前阶段不追求立即盈利，优先建立产品能力、可信度和真实用户，暂不开发用户支付系统，只作为长期学习和规划方向记录。
- 不得因为产品未来可能收费，就在当前阶段提前开发支付系统。

## 15. 角色分工与阶段验收

- Codex 是执行者和证据生产者，不是最终验收人。Codex 负责执行本地和生产操作、修改代码和文档、运行测试、生成 verification 证据、commit 和 push，并在完成后停止。
- Claude/GPT 独立验收对话负责阅读已 push 到 GitHub 的代码和文档，检查 commit 和 diff，交叉核对 verification 证据，检查矛盾、遗漏和夸大，并根据可访问证据给出通过、条件通过或阻断。
- Claude 网页版或其他仅连接 GitHub 的审计对话，不能声称直接查看了本地电脑、Cloudflare 实时控制台、实时 D1、Secrets 或本地环境变量。必须明确区分仓库可以证明的事实与无法独立验证的生产实时事实。
- 产品负责人负责决定产品优先级、批准执行范围、决定是否接受风险、决定阶段是否通过、决定是否进入下一阶段。
- 阶段完成流程固定为：Codex 执行；Codex 生成证据并 push；独立对话审计 GitHub 证据；产品负责人最终批准；未批准前不得进入下一阶段。
- 不得因为文档写着“完成”就自动认为阶段已经验收。

## 16. 防御性安全与攻击能力边界

- AI Workbench 必须建设防御能力，用于保护产品、用户、模型费用、密钥、安装包、供应链、生产环境和产品声誉。
- 允许和应当做的内容包括：密钥只保存在受控服务端、限流、钱包刹车、紧急关闭、输入内容安全检查、Prompt Injection 与恶意输入防护、依赖漏洞检查、公开 CVE 和安全公告跟踪、Windows 安装包完整性和篡改检测、Secret 扫描、日志脱敏、权限最小化、备份、灰度、回滚，以及使用公开安全标准和成熟防御工具。
- 明确禁止为攻击他人系统开发能力，禁止绕过他人的验证码、权限和访问控制，禁止开发渗透、盗取凭据或破坏性工具，禁止以“先学攻击才能防御”为理由实现可被直接用于攻击的能力，禁止把产品发展成具备主动攻击能力的工具。
- 允许了解公开的攻击思路和漏洞分析，用于制定防御措施。但任何实际实现都必须服务于保护自己的产品和用户，并保持合法、授权和可审计。

## 17. 极致零门槛与真正办成事

- 用户首次使用不应被要求理解模型、API、Token、Agent、Key 或复杂配置。
- 产品尽量做到安装后即可开始，复杂性留在后台；零门槛不等于隐藏风险或跳过用户同意。
- 不以“模型回复了一段文字”作为完成。判断完成要看用户目标是否真实达成。
- 需要执行时应执行，需要验证时应验证，失败时应说明原因和可恢复路径。
- 前端死守一个输入框和极简体验，不把后端调度复杂度暴露给用户，不为了展示功能增加大量按钮和面板。

## 18. 规模扩大时质量不能塌

- 用户增加不能成为降低质量、诚实程度和安全边界的理由。
- 每一步都要有检查、证据和失败边界；不能依靠夸大、隐藏问题或模糊告知维持表面增长。
- 产品要托住用户下限，也要提高用户上限：表达差、知识少、不懂技术或不会配置时，工作台负责理解、补全、解释和协助；高能力用户使用后，应能完成更复杂、更高质量、更大规模的任务。
- 产品不替代人的价值判断。用户负责方向、目标、取舍和最终决策，工作台负责把人的目标转化为可执行结果。

## 19. 工作台适应用户

- 不是让用户适应工作台，而是工作台理解并适应真实用户。
- 设计时必须检查是否要求用户学习过多技术术语，是否把模型、Key、环境和配置问题推给用户，是否可以由后台自动处理。
- 必须让用户操作时，先解释原因、说明好处和风险、获得明确同意，并允许用户拒绝、退出和恢复。
- 涉及强权限、数据记录、支付、自动执行系统操作、跨软件调用、文件和图片时，必须说明会做什么、为什么做、不会做什么、有什么风险、是否可关闭或撤回，以及出错后如何恢复。

## 20. 不给市场洗牌

- 产品不以消灭或替代其他模型、Agent 和工具为目标。
- 产品帮助优秀能力更顺畅地到达用户，加速生态连接和流通。
- AI Workbench 的核心资产是调度、监管、记忆、验证和交付，而不是封闭生态或重复造轮子。

## 21. 多 Agent 并行边界

- 多 Agent 可以用于并行研究、方案比较、测试用例设计、不同用户人格模拟、文档核对、UI 审查、安全审查和隔离补丁产出。
- 多 Agent 并行必须满足一个产品阶段、一个最终决策入口、统一 baseline、清楚划分文件和任务边界、每个 Agent 只负责一个模块、不得基于其他未合并分支继续开发、不得各自直接 push 到 main、由 Codex 统一集成、统一运行测试、统一形成候选版本。
- 产品负责人仍只对一个产品阶段作最终批准。多条线并行存在，不等于多个关键产品变更同时失控执行。

## 22. 执行快但不能免检

- 执行可以快，但结果不能免检。
- Agent 结果不得直接进入 main；Codex 必须统一集成并运行测试。
- GPT 审技术证据，Claude 审产品逻辑，产品负责人最终批准。
- 脚本通过不自动等于产品通过；真实产品、用户路径、风险边界和验收证据都必须检查。
