# GitHub Actions 预验收记录

Run ID：29919834193

URL：https://github.com/qingyueshen5-gif/AI-Workbench/actions/runs/29919834193

状态：failed

已知事实：

- Job：preflight
- `Build installer candidate`：success
- `Run install-release preflight`：skipped
- `Upload installer candidate and evidence`：success
- `Fail when preflight failed`：failure

根因判断：

- workflow 的 build 步使用 `continue-on-error` 后，后续 `if: steps.build.outcome == 'success'` 没有按预期让 preflight 执行。
- 本次失败不是安装包构建失败，也不是安装版 smoke-test 失败，而是 workflow 条件错误导致 preflight 被跳过。

处理：

- 已将 workflow 条件改为以 `release-v0.4.6-installer/AI-Workbench-Setup-v0.4.6-x64.exe` 是否真实存在作为 preflight 执行条件。
- 需要再次 push 触发新 run。
