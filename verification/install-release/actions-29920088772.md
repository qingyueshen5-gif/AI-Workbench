# GitHub Actions 预验收记录

Run ID：29920088772

URL：https://github.com/qingyueshen5-gif/AI-Workbench/actions/runs/29920088772

状态：failed

已知事实：

- `Build installer candidate`：success
- `Run install-release preflight`：skipped
- `Upload installer candidate and evidence`：success
- `Fail when preflight failed`：failure

根因判断：

- 使用安装包文件存在性作为 preflight 条件仍未按预期触发。
- 这次失败仍然是 workflow 条件导致核心 preflight 被跳过，不是安装器本地链路失败证据。

处理：

- 已改为 `if: always()`，无论 build 结果如何都执行 `npm run verify:install-release`。
- 如果没有安装包，preflight 会如实 failed 并生成 summary；如果安装包存在，preflight 必须真实执行安装、smoke-test 和卸载。
