# GitHub Actions 预验收记录

Run ID：29919498085

URL：https://github.com/qingyueshen5-gif/AI-Workbench/actions/runs/29919498085

状态：failed

已知事实：

- Job：preflight
- 失败步骤：Build installer candidate
- Checkout、Setup Node、Install dependencies 均为 success
- Run install-release preflight 未执行，状态 skipped
- Upload installer candidate and evidence 为 success

日志限制：

- `gh run view 29919498085 --log-failed` 返回 HTTP 403，需要仓库 admin 权限。
- `gh run download 29919498085` 返回 HTTP 401，当前 gh token 无效。

处理：

- 已更新 `.github/workflows/windows-installer-preflight.yml`，让 build 步骤把输出写入 `verification/install-release/actions-build.log`，并在失败时仍上传 artifact。
- 需要重新 push 触发新 run，取得可下载 artifact 后再定位云端 build 失败根因。
