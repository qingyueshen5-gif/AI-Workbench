# GitHub Actions 预验收记录

Run ID：29920336923

URL：https://github.com/qingyueshen5-gif/AI-Workbench/actions/runs/29920336923

状态：failed

已知事实：

- `Build installer candidate`：success
- `Run install-release preflight`：执行完成，但因 `continue-on-error` 显示为 success；最终 `Fail when preflight failed` 触发，说明 preflight 的真实 outcome 为 failure。
- `Upload installer candidate and evidence`：success
- `Fail when preflight failed`：failure

限制：

- 当前 `gh auth status` 显示 token invalid。
- `gh run view --log-failed` 返回 HTTP 403。
- `gh run download` 返回 HTTP 401。
- 因此本轮无法在本地读取 Actions artifact 中的 `preflight-summary.json` 或日志。

结论：

- 本地 R1.2 安装链路已通过。
- GitHub Actions 云端预验收仍 failed。
- 未取得 Actions passed 前，不得进入 3B，不得创建 Release/tag。
