# CQ-001 历史断言存档

```text
status=SUPERSEDED_BY_NON_EXECUTION_CLARIFY_CONTRACT
source=scripts/verify-task-lifecycle-001.mjs
historicalCase=clarification-requires-context-evidence
historicalInput=处理一下
```

## 原历史用例与断言

```js
{
  const { runtime, tasks } = makeRuntime({ interpretation: clarification() });
  const result = await runtime.handle({ messageId: 'clarify', originalMessageId: 'clarify', conversationId: 'clarify-c', chatId: 'clarify-c', text: '处理一下' });
  assert.equal(result.schedulerStatus, 'needs_clarification');
  assert.deepEqual((await tasks.load('clarify')).waitingFor.missingFields, ['target']);
  record('clarification-requires-context-evidence');
}
```

## 被正式产品契约取代的部分

- 创建业务Task；
- `waiting_for_clarification` Task状态；
- 通过`Task.waitingFor`承载现行clarify结果；
- 将clarify计入业务Task Lifecycle门禁。

现行契约为：

```text
decision=clarify
Task=0
Run=0
Scheduler=0
Provider=0
Model=0
```

## 仍保留为现行强制要求的部分

- `missingFields`必须非空；
- `questions`必须非空；
- 回复必须指出缺少的具体字段；
- 不得猜测路径、目标或先执行哪个复合意图；
- 不得启动执行或产生成功业务Final；
- 相同原始消息重投必须使用`messageReplayed=true`；
- 不得使用`taskReplayed`；
- 重复assistant追加为0；
- 外部交付最多一次。

现行专项：

```text
scripts/verify-non-execution-clarify-contract-001.mjs
```
