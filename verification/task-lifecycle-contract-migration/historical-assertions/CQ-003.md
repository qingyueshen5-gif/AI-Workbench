# CQ-003 历史断言存档

```text
status=SPLIT_INTO_INTERPRETER_FAILURE_AND_FAILED_TASK_REPLAY_CONTRACTS
source=scripts/verify-task-lifecycle-001.mjs
originalLines=171-178 at baseline 30afd0ae493929d8de3dd8ec8111c9e0cc60109f
historicalCase=invalid-interpreter-fails-not-clarification
cq003A=MIGRATED_AND_PASS
cq003B=COMPLETED_BY_RE_ANCHORED_CONTRACT
productDecision=RE_ANCHORED_TO_EXECUTION_STAGE_FAILURE_BY_PRODUCT_DECISION
```

## 原用例与原断言

```js
{
  const models = makeModels({ understand: async () => ({ text: '{bad' }) });
  const interpreter = new TaskInterpreter({ model: models });
  const tasks = new TaskStore({ root: join(root, 'correction-fail') });
  const runtime = new AgentRuntime({ root, allowedRoots: [root], tasks, sessions: new ContractSessionStore(), models, tools: makeTools(), verifier: new ResultVerifier(), taskInterpreter: interpreter });
  await assert.rejects(() => runtime.handle({ messageId: 'bad', originalMessageId: 'bad', conversationId: 'bad-c', chatId: 'bad-c', text: 'hello' }), /Task Interpreter/);
  assert.equal((await tasks.load('bad')).currentState, 'failed');
  record('invalid-interpreter-fails-not-clarification');
}
```

## 原测试目的

原用例同时验证：

1. TaskInterpreter第一次和唯一一次纠正均无效时，正式抛出Task Interpreter错误，而不是伪装为clarification；
2. Runtime已存在的业务Task通过正式错误路径持久化为`failed`。

## 为什么必须拆分

第一部分属于TaskInterpreter组件的调用次数、受限纠正和错误语义；第二部分属于Runtime的Task创建时机、状态机持久化和终态重放。两者处于不同架构边界，合并测试会把组件失败错误地绑定到历史Runtime入口。

## CQ-003-A映射

专项：

```text
scripts/verify-task-interpreter-bounded-correction-failure-001.mjs
```

| 原安全语义 | CQ-003-A |
|---|---|
| 两次均非法 | Fixture第一次缺字段，第二次数组类型非法 |
| 只允许一次纠正 | 模型调用次数精确为2，第三次立即抛错 |
| `/Task Interpreter/` | 正式`TaskInterpretationError`且message匹配`/Task Interpreter/` |
| 不是clarification | 无clarification分类或结果，`clarification=false` |
| 不进入执行链 | Scheduler/Provider/Task/Run/Final均为0 |
| 模型不得控制内部协议 | 两次额外`providerId/approved/authorizationContext`均未进入观察到的错误协议 |

状态：

```text
CQ-003-A=MIGRATED_AND_PASS
```

## CQ-003-B映射与产品重新锚定

原断言：

```js
assert.equal((await tasks.load('bad')).currentState, 'failed');
```

原计划映射到：

```text
Task真实创建
→ interpreting
→ TaskInterpreter失败
→ failTask()
→ currentState=failed
→ 同一Task终态重放
```

控制流核查结论：

```text
B. HISTORICAL_CONTRACT_DRIFT
```

证据：

```text
verification/task-lifecycle-contract-migration/CQ-003B-CONTROL-FLOW-FEASIBILITY.md
verification/task-lifecycle-contract-migration/cq-003b-control-flow-feasibility.json
```

当前B2 Runtime在Task创建前由确定性InterpreterAdapter决策；Task创建后的`interpreting`只使用`adapterResult.taskDraft`，没有可达的`TaskInterpreter.interpret()`。因此原failed Task断言属于B2前历史契约。该结论继续保持：

```text
B. HISTORICAL_CONTRACT_DRIFT
```

产品负责人随后作出：

```text
RE_ANCHORED_TO_EXECUTION_STAGE_FAILURE_BY_PRODUCT_DECISION
```

正式拆分后的覆盖为：

- CQ-003-A：保护TaskInterpreter错误不得被伪装为clarification，状态`COMPLETED / PASS`；
- CQ-003-B'：保护真实业务Task在当前可达执行阶段失败后的结构化失败事实持久化与终态幂等重放，正式名称`EXECUTION_STAGE_FAILED_TASK_PERSISTENCE_AND_REPLAY`。

原“TaskInterpreter失败后写入failed Task”的特定控制流已被现行架构取代，不再属于现行门禁。本轮没有声称旧控制流仍被执行；原失败来源已经改变，但通用安全语义获得当前架构的新覆盖。

CQ-003-B'专项：

```text
scripts/verify-failed-task-persistence-and-replay-001.mjs
```

状态：

```text
CQ-003-B'=COMPLETED_BY_RE_ANCHORED_CONTRACT
failureFactPersistence=PASS
terminalTaskReplay=PASS
failedReplayVerified=false
taskReplayed=true
messageReplayed=false
```

本轮仍没有：

- 恢复旧chat Task路径；
- 提前创建占位Task；
- 手工写failed状态；
- 恢复不可达的TaskInterpreter Runtime入口。

## 覆盖完整性

- 原`/Task Interpreter/`错误断言由CQ-003-A覆盖；
- 原failed Task通用安全语义由CQ-003-B'在当前执行阶段失败边界覆盖；
- `B. HISTORICAL_CONTRACT_DRIFT`被保留，未改写为历史用例原样通过；
- 原TaskInterpreter特定失败来源没有被伪造为仍存在。
