# AW-AILINK-ROUTE-AND-REVIEW-001｜AW-AILINK-GROUP-001版本3方案只读审查

- 目标工作流：`workflow-3da933e691b5`
- 业务任务：`AW-AILINK-GROUP-001`
- 状态：`review_ready / review`
- 草稿版本：3
- 核验性质：只读文件和已安装运行逻辑审查
- 方案修改：未执行
- 方案确认：未执行
- 新生成：未执行
- 审查分类：`review_passed_with_nonblocking_notes`
- 新生成结论：`new_generation_not_required`

## 1. 工作群目标

方案目标为：

- 先在旧电脑完成权威文档、任务台账、工作包、依赖、预算和验收准备；
- 待新电脑迁移与环境验收通过，并预先定义验收矩阵后；
- A架构、E隐私和G测试角色在独立边界并行实施；
- 最终仅由总集成审查、集成、回归并进入人工主线合并审批。

方案不是立即开工指令，包含正式开工审批门和主线合并审批门。

## 2. 完整结构摘要

### 五个角色

1. **协调监工**
   - 读取并引用权威文档；
   - 建立任务台账和A/E/G工作包；
   - 明确边界、依赖、预算和验收条件；
   - 通过飞书原生@调度；
   - 跟踪时间、状态、费用和证据；
   - 遇高风险、预算超限或权威冲突时暂停报告；
   - 不写业务代码、不合并主线。

2. **A架构开发**
   - 负责公共底层模块1–3；
   - 在独立worktree实施统一契约、状态、错误、取消、重试、Provider capability和本地mock；
   - 不直接修改或合并主线。

3. **E隐私开发**
   - 负责隐私与反馈模块4、13；
   - 在独立worktree实施最小日志隐私Schema、告知、关闭、清除和反馈入口；
   - 不直接修改或合并主线。

4. **G测试验收**
   - 预建可追溯验收矩阵；
   - 负责迁移、虚拟人格、安全、成本、恢复及A/E工作包验收；
   - 不代替开发角色修改实现，不合并主线。

5. **总集成**
   - 只在A/E/G全部通过后启动；
   - 审查diff、测试、证据、Secret、依赖、冲突和越权修改；
   - 集成候选成果并运行全量回归；
   - 生成待合并结果并提交第二个人工审批；
   - 获得产品负责人批准后才可合并主线；
   - 不代写A/E/G代码，不部署生产，不扩大到其他工作包。

五个计划角色全部存在，名称完全符合计划。

### 工作流程

方案包含12个步骤：

1. 准备阶段；
2. G测试建设；
3. 开工人工审批；
4. A/E/G并行启动；
5. A架构开发；
6. E隐私开发；
7. G统一验收；
8. 工作包停止报告；
9. 总集成全量回归；
10. 回归失败报告；
11. 主线合并人工审批；
12. 合并与群内交付。

包含16条流转关系和2个人工审批规则。

## 3. 交接关系和最终集成权

- 唯一入口角色：`coordination-supervisor`。
- 协调监工统一准备、启动和调度A/E/G。
- A和E将工作包及证据提交给G验收。
- G首次不通过可退回相应开发角色一次；第二次仍失败则停止并报告。
- A/E/G全部通过后进入总集成。
- 总集成发现责任缺陷时只能退回原责任角色一次，不能直接代写。
- 最终合并步骤唯一拥有者为`chief-integrator`。
- 两个正式审批门的审批人均为`product_owner`。

方案明确多Agent可以并行，但只有总集成拥有最终集成权，只有产品负责人拥有最终阶段批准权。没有多个员工各自合并、发布或决定下一阶段。

## 4. 群响应策略

已安装AI Link运行逻辑明确：

- 只有飞书原生@目标员工才开始模型任务；
- 未被@的员工不回复、不调用模型；
- 每个员工根据自己的roleId执行；
- Agent交接使用原生@和结构化信封；
- 群专属Skill绑定chatId；
- 编译后的trigger为`manual`且`requiresMention=true`；
- 总集成负责最终汇总和交付。

因此群响应策略符合“群响应开启、默认需要@、降低无关调用和费用、独立角色上下文、总集成汇总”的要求。

## 5. 权限边界

### 合格部分

方案明确禁止或限制：

- 协调监工修改业务代码；
- A、E、G直接合并主线；
- 总集成代写其他角色代码；
- 未经审批合并主线；
- 生产部署；
- 擅自扩大到B/C/D/F工作包；
- 隐藏失败证据；
- 预算超限后继续；
- Secret越权处理；
- 自动无限返工。

正式开发动作被放在独立worktree、范围和人工开工审批之后。当前review状态本身不会执行这些动作。

### 非阻塞说明

方案是未来执行蓝图，因此包含正式开发、Codex调用、worktree和主线合并步骤；但这些步骤均在明确审批门后，并非“确认方案即立即开发”。确认方案后AI Link的下一产品动作是准备员工，后续仍需绑定和建群确认。产品负责人在执行时仍应保持当前阶段门，不把方案确认解释为正式开工批准。

方案未默认允许支付、修改Secret、删除数据、账号修改或生产部署。费用规则为6.4元预警、8元停止，属于限制而非授权。

## 6. 产品定位

- 草稿没有出现DeepSeek或把产品写成DeepSeek客户端。
- A角色职责明确包含`provider capability`。
- 方案通过AI Link受控模型通道和Codex工具描述执行实现，但没有把单一Provider定义为产品本体。
- 与`PRODUCT.md`中“模型、Agent和工具无关的调度产品；DeepSeek只是当前可替换执行者”一致。

产品定位判定：模型中立，合格。

## 7. 审查结论

最终分类：`review_passed_with_nonblocking_notes`

通过原因：

- 五个计划角色齐全；
- 单入口、并行支线、统一G验收、单一总集成明确；
- 两个产品负责人人工审批门明确；
- 群消息默认需要原生@；
- 生产部署、越权合并、扩大范围和代写均被禁止；
- 产品定位模型中立；
- 失败退回、停止和证据要求明确；
- 方案主体结构完整，不需要重新生成。

非阻塞说明：

1. 当前阶段只批准方案接管审查，不代表批准正式开发；后续人工确认方案时应再次声明“确认方案不等于开工审批”。
2. 协调监工和G测试验收的assignment.mode当前仍为空；这是确认方案前的员工分配UI状态，不是蓝图结构错误，且不应在本轮修改。
3. “模型调用只走AI Link/18800受控通道”与当前AI Link本机端口18765/18766、AI Workbench仓库18800存在命名/执行域差异。它不破坏角色和流程，但在正式开工审批前应由人工文档或任务卡明确各环境实际入口，避免执行者误用。

## 8. 是否需要修改或重新生成

- 是否需要人工修改现有草稿：**创建前非必需**；上述说明可在确认审批或后续任务卡中补充，不需要调用模型。
- 是否需要重新生成：**否**。
- 明确结论：`new_generation_not_required`。

## 9. 工作流重复和运行状态复核

- 工作流目录数量：2；
- `workflow-3da933e691b5`存在；
- `draftRevision=3`；
- `status=review_ready`；
- `stage=review`；
- `pendingBlueprint=null`；
- `lastError=null`；
- 没有第三个同目标工作流；
- 没有生成中workflow状态；
- candidate中的`running=true`仅表示候选员工进程在线，不是工作流生成请求运行中。

## 10. 本轮边界

没有修改草稿，没有调用“让创建工程师调整”，没有确认方案，没有准备员工，没有绑定飞书，没有创建群，没有生成新方案。
