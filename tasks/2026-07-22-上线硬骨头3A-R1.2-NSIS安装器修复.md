# 上线硬骨头3A-R1.2：NSIS安装器修复

日期：2026-07-22

## 任务

定位并修复 v0.4.6 Windows NSIS 安装器默认安装不落盘问题。

## 根因

- `win-unpacked` 可运行，安装包 payload 有效。
- 显式 `/D=C:\Users\Public\AIW-R1-2-Test\AI Workbench` 可真实落盘。
- 默认 per-user 安装目录在当前中文用户名环境下没有稳定落盘，表现为只留下 `%LOCALAPPDATA%\ai-workbench-updater\installer.exe`，没有创建真实安装目录、卸载器和注册表项。

## 修复

- 新增 `build/installer.nsh`，将默认安装目录固定为 `%LOCALAPPDATA%\Programs\AIWorkbench`。
- 新增 `scripts/verify-nsis-install.mjs`，真实执行 NSIS `/S` 安装、安装版 `--smoke-test` 和卸载。
- 更新 `scripts/verify-install-release.mjs`，主 preflight 改用 Node helper。
- 更新 `.github/workflows/windows-installer-preflight.yml`，失败时也上传安装包、builder-debug 和 verification 证据。

## 验收

- 本地 `npm.cmd run verify:install-release`：passed。
- NSIS `/S` 安装真实落盘：passed。
- `AI Workbench.exe` 存在：passed。
- 卸载器存在：passed。
- 卸载注册表项存在：passed。
- 桌面/开始菜单快捷方式指向本次安装目录：passed。
- 安装版 `--smoke-test` 退出码 0：passed。
- 真实卸载退出码 0：passed。
- `0x80000003`：未复现。

## 证据

- `verification/install-release/repair1-2-summary.json`
- `verification/install-release/repair1-2-report.md`
- `verification/install-release/repair1-2-install.log`
- `verification/install-release/repair1-2-smoke.log`
- `verification/install-release/repair1-2-uninstall.log`
- `verification/install-release/preflight-summary.json`
- `verification/install-release/preflight-report.md`

## 状态

本地 R1.2 通过；GitHub Actions 真实结果待 push 后确认。

不得创建 Release/tag，不得自动进入 3B。`shared_managed` 生产注入仍为 blocked，本轮不处理。
