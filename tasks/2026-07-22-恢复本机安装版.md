# 恢复本机 AI Workbench 安装版

日期：2026-07-23 00:19 +08:00

## 任务性质

本轮是本机日常使用环境恢复，不改变当前产品主线。

- 不进入 `③A-R2`
- 不进入 `③B`
- 不创建 Release 或 tag
- 不修改 `shared_managed`
- 不修改代码
- 不执行卸载

## 当前事实

- `③A-R1.3` 已通过。
- GitHub Actions Run `29935231224` 已验证候选安装包通过云端 build/install/smoke/uninstall/扫描。
- 通过验收的候选包 SHA256 为 `ca833403906e8ba82c267813ced701b39a83f9d7a7d9f3e9e857a011b6b9ab47`。

## 执行记录

1. 检查本地候选包：
   - 路径：`F:\AI-Workbench\release-v0.4.6-installer\AI-Workbench-Setup-v0.4.6-x64.exe`
   - 初始本地 SHA256：`857fd37521fa6cf8a01306d165e4cf3b5123013ca8392f1acf92807214976992`
   - 结论：本地文件不是 Run `29935231224` 通过验收的候选包。

2. 下载并恢复通过验收的候选包：
   - 来源：GitHub Actions Run `29935231224` artifact
   - 下载包 SHA256：`ca833403906e8ba82c267813ced701b39a83f9d7a7d9f3e9e857a011b6b9ab47`
   - 已覆盖到：`F:\AI-Workbench\release-v0.4.6-installer\AI-Workbench-Setup-v0.4.6-x64.exe`

3. 安装：
   - 直接 `/S` 安装未在本机稳定落盘。
   - 使用 NSIS 显式目录安装：
     `AI-Workbench-Setup-v0.4.6-x64.exe /S /D=%LOCALAPPDATA%\Programs\AIWorkbench`
   - 退出码：0

4. 安装结果：
   - 安装目录：`C:\Users\胖胖虎\AppData\Local\Programs\AIWorkbench`
   - `AI Workbench.exe`：存在
   - 版本：`0.4.6.0`
   - 用户数据目录：`C:\Users\胖胖虎\AppData\Roaming\ai-workbench` 存在，未删除。

5. 快捷方式：
   - 安装后快捷方式曾指向 `C:\Users\CodexSandboxOffline\AppData\Local\Programs\AIWorkbench\AI Workbench.exe`。
   - 已手动修正桌面和开始菜单快捷方式目标到当前真实安装目录：
     `C:\Users\胖胖虎\AppData\Local\Programs\AIWorkbench\AI Workbench.exe`
   - 最终使用外部权限环境复核：`envUser=胖胖虎`，桌面和开始菜单快捷方式均指向当前真实安装目录。

6. 启动验证：
   - 安装版 `--smoke-test` 退出码：0
   - 正常启动后检测到 `AI Workbench` 进程。

## 结果

- 安装：通过
- 桌面快捷方式：通过，已修正到当前安装目录
- 开始菜单快捷方式：通过，已修正到当前安装目录
- 应用启动：通过
- 版本 `0.4.6`：通过
- 用户数据保留：通过

## 已知本机现象

本机直接 `/S` 安装未稳定落盘；显式 `/D=%LOCALAPPDATA%\Programs\AIWorkbench` 可成功安装。安装/启动过程中快捷方式可能被写成 sandbox 用户路径，本轮已在最终状态中修正。普通沙箱视角读取快捷方式时可能仍显示 `CodexSandboxOffline`，最终状态以外部权限环境复核为准。

该现象只记录为本机恢复安装注意事项，不改变 `③A-R1.3` 云端预验收结论。
