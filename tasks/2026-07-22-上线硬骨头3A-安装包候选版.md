# 2026-07-22 上线硬骨头3A：安装包候选版

## 任务

生成 Windows x64 安装包候选版，并完成发布前预验收。只做 3A，不创建 GitHub Release，不创建正式 tag，不进入 3B。

## 做了什么

- 将候选版本更新为 `0.4.6`。
- 复用现有 Electron + electron-builder + NSIS 打包链路。
- 设置候选安装包名称：`AI-Workbench-Setup-v0.4.6-x64.exe`。
- 将候选安装包输出到 `release-v0.4.6-installer/`。
- 新增 Electron `--smoke-test` / `AIW_SMOKE_TEST=1` 用户不可见验收入口。
- 新增 `npm run verify:install-release`，生成安装包预验收 summary/report。
- 新增 GitHub Actions 预验收工作流 `.github/workflows/windows-installer-preflight.yml`，但本地尚未取得真实远端 Actions 运行结果。

## 验收产物

- `verification/install-release/preflight-summary.json`
- `verification/install-release/preflight-report.md`
- `verification/install-release/nsis-install-uninstall.json`

## 候选安装包

- 本地路径：`release-v0.4.6-installer/AI-Workbench-Setup-v0.4.6-x64.exe`
- SHA256：`90b9a6c30e015fe8a283eae0ae31909c330511f372660596f4905b52b735adf7`
- 说明：候选 exe 不提交 Git。

## 结果

状态：failed。

五条硬验收中：

- a. 无硬编码开发机路径：passed
- b. 首次运行自建目录：passed
- c. 依赖缺失不崩并给中文说明：passed
- d. 端口冲突有兜底：passed
- e. 就绪报告完整：passed

但 3A 总体验收失败，因为：

- NSIS 静默安装退出码为 0，但未在预期 per-user Programs 目录创建已安装 exe 和 uninstaller。
- 现有桌面/开始菜单快捷方式仍指向旧桌面安装路径。
- 卸载未执行，因为未找到预期 uninstaller。
- packaged Electron smoke test 未成功完成，退出码为 `2147483651`。
- `shared_managed` 只完成 mock 机制测试，生产注入验证为 blocked。

## 下一步

不能进入 3B。下一轮应先修复 NSIS 安装目标/旧安装记录处理、packaged smoke-test 崩溃、生产 shared key 注入证据，再重新跑 3A。
