# 上线硬骨头3A-R2.0：共享 Key 架构核验

日期：2026-07-23

## 任务

不改代码，真实审计当前 `shared_managed` 链路，锁定生产方案。

## 结果

状态：passed。

这是架构核验 passed，不是生产注入 passed。

## 做了什么

- 读取 `EXECUTION_PROTOCOL.md`、`PRODUCT.md`、`LAUNCH.md`、`CURRENT_TASK.md`、`TASKLOG.md`、`NEXT_STEP.md`、`DECISIONS.md`。
- 读取 `model-proxy.mjs`、`verification/shared-key/summary.json`、`verification/daily-closeout/summary.json`。
- 读取 `research/self-hosting-plan.md` 和 `research/unified-model-proxy-plan.md`。
- 审计 `model-proxy.mjs` 的 key 读取顺序、`.env` 读取、loopback 限制、`/health` 暴露字段和上游转发方式。
- 确认当前 `shared_managed` 仍是本机环境兜底，不是远程托管生产注入。
- 锁定正式架构：本机 18800 -> AI Workbench 自控远程 Managed Proxy -> DeepSeek 官方 API。
- 明确不采用“真实 Key 随包分发 + 消费限额”。

## 验收产物

- `research/managed-proxy-production-plan.md`
- `verification/managed-shared-key/architecture-summary.json`
- `verification/managed-shared-key/architecture-report.md`

## 下一步

下一次唯一任务是 `③A-R2.1：实现远程 Managed Proxy 并做真实生产注入验证`。

不进入 Release、首屏示例、模型分层、手机端或情报流水线。
