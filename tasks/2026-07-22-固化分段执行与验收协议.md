# 2026-07-22 固化分段执行与验收协议

## 任务

在继续上线硬骨头3A前，先把 GPT / Codex / Claude / 其他执行助手的固定协作和验收习惯写入仓库，避免后续依赖聊天框临时补充。

## 做了什么

- 新增根目录 `EXECUTION_PROTOCOL.md`。
- 明确角色分工：产品负责人拍板，GPT 规划和检查，Codex 执行和留痕。
- 明确唯一事实来源：`F:\AI-Workbench` 和 GitHub `origin/main`。
- 固化单一主线、分段执行、真实验收、失败也留痕、产品负责人批准后才能进入下一阶段。
- 将 `EXECUTION_PROTOCOL.md` 加入交接必读清单。

## 对当前主线的影响

不改变产品主线，不新增产品功能。

当前唯一主线仍是：

```text
上线硬骨头3A：生成 Windows 安装包候选版并完成发布前预验收。
```

本轮仍禁止：

- 创建 GitHub Release；
- 创建正式 tag；
- 进入 3B；
- 做模型分层；
- 创建 `verification/model-router/summary.json`。

## 验收产物

- `EXECUTION_PROTOCOL.md`
- 本任务记录文件

## 状态

已完成。后续所有大任务必须先读 `EXECUTION_PROTOCOL.md`。
