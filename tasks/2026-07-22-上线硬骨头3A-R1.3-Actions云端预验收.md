# 上线硬骨头3A-R1.3：Actions 云端预验收

日期：2026-07-22

## 任务

恢复 GitHub Actions 可观测性，读取 Run `29920336923` 的失败日志和 artifact，定位云端 preflight 失败原因，最小修复后取得一次真实 success。

## 当前结果

状态：pending。

## 做了什么

- 读取 `EXECUTION_PROTOCOL.md`、当前任务文档、R1.2 验收证据、Actions 记录、workflow 和安装验证脚本。
- 确认本地 R1.2 安装链路为 passed。
- 检查仓库状态：开始时本地 `HEAD` 与 `origin/main` 一致。
- 恢复 GitHub CLI 后读取 Run `29920336923` 摘要和 artifact。
- 下载 artifact `ai-workbench-v0.4.6-installer-preflight`，Artifact ID `8529650090`。
- 从 `actions-build.log` 定位真实根因：`package.json` 写死 `build.electronDist=node_modules/electron/dist`，Actions 环境中该目录不存在，electron-builder 未产出安装包。
- 修复 `package.json`，删除 `electronDist`。
- 修复 `scripts/verify-install-release.mjs`，避免 artifact 缺失时读取旧 NSIS 证据，并让安全扫描跳过不可读目录。
- 修复 `scripts/verify-nsis-install.mjs`，smoke runtime 目录改为每次唯一。
- 新增 `scripts/clean-release-output.mjs` 并接入 `dist:win`。
- 更新 workflow Step Summary 和 final gate。
- 本地 `npm.cmd run verify:install-release` 已完成真实安装、smoke-test、卸载和扫描；因本机旧 `win-unpacked` 被前序文件锁清理破坏，结果仍 failed，云端干净环境以新 Actions run 为准。

## 根因

Run `29920336923` 的 `actions-build.log`：

```text
The specified electronDist does not exist: D:\a\AI-Workbench\AI-Workbench\node_modules\electron\dist
```

这导致云端没有生成安装包和 `win-unpacked`，`preflight-summary.json` 中 `artifactExists=false`、`unpackedExists=false`、`sha256=""`。

## 验收产物

- `verification/install-release/repair1-3-summary.json`
- `verification/install-release/repair1-3-report.md`

## 下一步

提交并 push 后继续：

1. 触发新的 `Windows Installer Preflight`。
2. 等待新 run 完成。
3. 下载新 run artifact。
4. 只有新 run conclusion 为 `success`，才能将 R1.3 写为 passed。
